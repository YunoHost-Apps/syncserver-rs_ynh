Following the upstream changes this version now:

- Requires Python 3.13 - free on `trixie`, installed via `uv` on `bookworm` (no user action needed)
- drops `i686` support as I have no clue where to take Python 3.13 for said arch